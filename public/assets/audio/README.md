# Audio asset slot

`bgm_main.mp3` may be placed here once the project has a licensed, original,
or otherwise permitted looping background track. Until then `AudioManager`
deliberately keeps BGM silent while its settings and lifecycle behaviour remain
fully functional.
