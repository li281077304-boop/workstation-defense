# TODO — 下一步

> 按优先级排序。做之前先读 `../AGENTS.md` + `LOCKED_RULES.md`。
> 完成一项 → 勾选并 commit（`docs: update TODO`）。

---

## P0（当前阶段：让"像游戏"）

- [x] **规则与前期循环修复**：出生槽仅来自奖励球捕获、敌人 footprint 防重叠、事件顺序回放、无跳变呼吸动画、轻量开局（2026-08-28）。
- [x] **战斗同步与调参工具**：修正敌人/子弹格坐标同步，奖励球生命周期事件化，新增本地 Difficulty Debug Panel 与 `DIFFICULTY.md`（2026-08-28）。
- [x] **RewardBall 推进与难度观测**：奖励球从最后列随敌人推进；新增利用率、奖励获取率、战场压力与滚动调试读数，不启用自动调参（2026-08-28）。
- [x] **可解性 Spawn Safety Guard**：统一 Enemy/RewardBall 占位；新怪经 required utilization 与整批压力检查后才出生；大怪概率平滑解锁（2026-08-28）。
- [x] **可玩基线与局后记录**：冻结 PLAYABLE_BASELINE_V1，Debug Panel 增加一键恢复及每局结束指标摘要（2026-08-28）。
- [x] **落位/合成动画**：植物落位 Scale 回弹 + 合成白闪（playPlacementAnimation，2026-08-27 完成）。
- [x] **战斗反馈**：捕获奖励金色闪光 + "+N" 飘字、击杀烟尘、命中绿闪、出生槽刷新弹跳（完成）。
- [x] **手感修复**：input 监听器去重（重开不卡）、拖拽 ghost 跟随（完成）。
- [x] **奖励球自然生成**：开启 + 上限 5 个（完成）。
- [ ] **合成反馈再强化**：当前白闪偏弱，可加"合成值飘字"（如 `+8`）让合成更明显。

## P1（可玩性闭环）

- [ ] **难度曲线实测**：`enemySpawnChance / enemyHpGrowthPerTurn` 按实玩调整，
      目标"先能持续玩"（文档 §29）。需要用户试玩反馈。
- [ ] **奖励球生成位置**：当前 `col≥3` 随机；实玩观察是否"堵死出生槽成长"。
- [ ] **开场教学感**：玩家可能不知道"拖出生槽→空格、同值合成"，可加一行短暂提示。

## P2（表现完善）

- [x] 死亡烟尘特效接入（effect-smoke-01，完成）。
- [x] 子弹错峰沿 Lane 排列（完成）。
- [x] **基础音效**：合成、命中、击杀、出生槽刷新已用 Phaser WebAudio 合成（2026-08-27）。
- [ ] **回合节奏微调**：当前 ~790ms（落位170+子弹420+推进200），目标 650~900ms，实测后微调。

## P3（外围，暂不做——文档 §47 明确禁止）

- [ ] 关卡/商店/抽卡/升级树/能量/生命/倍速/排行榜/任务/成就 —— **不要做**，除非用户明确要求。

---

## 待用户决策的事项

- [ ] **美术**：明天 GPT 生成后替换 `assets/` 下 PNG（每张独立完整图，直接覆盖即可）。
- [x] **GitHub 推送**：私有远程 `origin` 已配置，`main` 已推送。
- [ ] **安卓打包**：本机缺 SDK/JDK 环境，装了 Android Studio 后可 `npm run android:sync` 出包。

---

## 未决问题（见 OPEN_QUESTIONS.md / PROJECT_HANDOFF.md）

- 同格"敌人 vs 奖励球"谁先挨打：当前保守设"奖励球优先阻挡"，未实测。
- 自然生成 16 以上奖励球：未确认，做成配置项（`maxNaturalSpawnValue`）。
